<Window x:Class="Source_Load_Test.View.LoadListModeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Load 리스트 모드 (List Mode) - Electronic Load"
        Height="930"
        Width="880"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White">

    <Window.Resources>
        <Style x:Key="SectionHeaderStyle"
               TargetType="Border">
            <Setter Property="Background"
                    Value="#E3F2FD" />
            <Setter Property="BorderBrush"
                    Value="#2196F3" />
            <Setter Property="BorderThickness"
                    Value="0,0,0,2" />
            <Setter Property="Padding"
                    Value="10,8" />
            <Setter Property="Margin"
                    Value="0,0,0,10" />
        </Style>

        <Style x:Key="ActionButtonStyle"
               TargetType="Button">
            <Setter Property="Height"
                    Value="35" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="FontSize"
                    Value="12" />
            <Setter Property="Cursor"
                    Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 상단 안내 메시지 -->
        <Border Grid.Row="0"
                Background="#E8F5E9"
                BorderBrush="#4CAF50"
                BorderThickness="2"
                Padding="12"
                Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Orientation="Horizontal"
                            Margin="0,0,0,5">
                    <TextBlock Text="📋 "
                               FontSize="16" />
                    <TextBlock Text="리스트 모드란?"
                               FontWeight="Bold"
                               FontSize="13"
                               Foreground="#2E7D32"
                               VerticalAlignment="Center" />
                </StackPanel>
                <TextBlock FontSize="11"
                           Foreground="#388E3C"
                           TextWrapping="Wrap"
                           LineHeight="16">
                    <Run Text="미리 정의된 전류값 시퀀스를 자동으로 순차 실행합니다." />
                    <LineBreak />
                    <Run Text="예) DUT(전원장치) 부하 변동 테스트: 1A → 3A → 5A → 2A → 반복" />
                    <LineBreak />
                    <Run Text="각 스텝마다 지속 시간을 설정할 수 있습니다." />
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- 리스트 선택 영역 -->
        <Border Grid.Row="1"
                BorderBrush="#2196F3"
                BorderThickness="2"
                Padding="10"
                Margin="0,0,0,10"
                Background="White">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 라벨 -->
                <TextBlock Grid.Column="0"
                           Text="저장된 리스트:"
                           FontWeight="Bold"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0" />

                <!-- 리스트 선택 ComboBox -->
                <ComboBox Grid.Column="1"
                          ItemsSource="{Binding SavedLists}"
                          SelectedItem="{Binding SelectedList}"                         
                          Height="35"
                          FontSize="12"
                          VerticalContentAlignment="Center"
                          Margin="0,0,10,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding ListNumber}"
                                           FontWeight="Bold"
                                           Foreground="#1976D2"
                                           Margin="0,0,5,0" />
                                <TextBlock Text="-"
                                           Foreground="Gray"
                                           Margin="0,0,5,0" />
                                <TextBlock Text="{Binding ListName}"
                                           Margin="0,0,10,0" />
                                <TextBlock Text="("
                                           Foreground="Gray" />
                                <TextBlock Text="{Binding StepCount}"
                                           Foreground="Gray" />
                                <TextBlock Text=" steps)"
                                           Foreground="Gray" />
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <!-- 불러오기 버튼 -->
                <Button Grid.Column="2"
                        Content="📥 불러오기"
                        Background="#2196F3"
                        Foreground="White"
                        FontWeight="Bold"
                        Width="100"
                        Height="35"
                        Margin="0,0,5,0"
                        Command="{Binding LoadListCommand}"
                        IsEnabled="{Binding SelectedList, Converter={StaticResource BoolToColorConverter}}" />

                <!-- 새 리스트 버튼 -->
                <Button Grid.Column="3"
                        Content="➕ 새 리스트"
                        Background="#4CAF50"
                        Foreground="White"
                        FontWeight="Bold"
                        Width="100"
                        Height="35"
                        Command="{Binding CreateNewListCommand}" />
            </Grid>
        </Border>

        <!-- 메인 설정 영역 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="200" />
            </Grid.ColumnDefinitions>

            <!-- 왼쪽: 리스트 테이블 -->
            <Border Grid.Column="0"
                    BorderBrush="#BDBDBD"
                    BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- 테이블 헤더 -->
                    <Border Grid.Row="0"
                            Background="#2196F3"
                            Padding="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="Step"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       FontSize="12" />
                            <TextBlock Grid.Column="1"
                                       Text="Mode"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       FontSize="12" />
                            <TextBlock Grid.Column="2"
                                       Text="Value (V, C, R)"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       FontSize="12" />
                            <TextBlock Grid.Column="3"
                                       Text="Duration (s)"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       FontSize="12" />
                            <TextBlock Grid.Column="4"
                                       Text="Action"
                                       Foreground="White"
                                       FontWeight="Bold"
                                       FontSize="12"
                                       HorizontalAlignment="Center" />
                        </Grid>
                    </Border>

                    <!-- 리스트 항목 (DataGrid) -->
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding ListSteps}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              SelectionMode="Single"
                              SelectedItem="{Binding SelectedStep}"
                              HeadersVisibility="None"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#E0E0E0"
                              BorderThickness="0">
                        <DataGrid.Columns>
                            <!-- Step 번호 -->
                            <DataGridTextColumn Header="Step"
                                                Width="50"
                                                Binding="{Binding StepNumber}"
                                                IsReadOnly="True">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment"
                                                Value="Center" />
                                        <Setter Property="VerticalAlignment"
                                                Value="Center" />
                                        <Setter Property="FontWeight"
                                                Value="Bold" />
                                        <Setter Property="Foreground"
                                                Value="#1976D2" />
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- 모드 -->
                            <DataGridComboBoxColumn Header="Mode"
                                                    Width="50"
                                                    SelectedValueBinding="{Binding Mode, UpdateSourceTrigger=PropertyChanged}"
                                                    IsReadOnly="False">
                                <DataGridComboBoxColumn.ItemsSource>
                                    <x:Array Type="{x:Type system:String}"
                                             xmlns:system="clr-namespace:System;assembly=mscorlib">
                                        <system:String>CCH</system:String>
                                        <system:String>CV</system:String>
                                        <system:String>CPH</system:String>
                                        <system:String>CRL</system:String>
                                    </x:Array>
                                </DataGridComboBoxColumn.ItemsSource>
                            </DataGridComboBoxColumn>
                            <!-- 전류값 → Value로 변경 -->
                            <DataGridTemplateColumn Header="Value"
                                                    Width="170">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox Grid.Column="0"
                                                     Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                                     BorderThickness="0"
                                                     Padding="8,5"
                                                     FontSize="12"
                                                     HorizontalContentAlignment="Right" />
                                            <TextBlock Grid.Column="1"
                                                       Text="V/A/Ω"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"
                                                       Foreground="Gray" />
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Duration → Time으로 변경 -->
                            <DataGridTemplateColumn Header="Time"
                                                    Width="140">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox Grid.Column="0"
                                                     Text="{Binding Time, UpdateSourceTrigger=PropertyChanged}"
                                                     BorderThickness="0"
                                                     Padding="8,5"
                                                     FontSize="12"
                                                     HorizontalContentAlignment="Right" />
                                            <TextBlock Grid.Column="1"
                                                       Text=" s"
                                                       VerticalAlignment="Center"
                                                       Margin="0,0,8,0"
                                                       Foreground="Gray" />
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- 삭제 버튼 -->
                            <DataGridTemplateColumn Header="Action"
                                                    Width="270">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="✕"
                                                Background="#FFEBEE"
                                                Foreground="#C62828"
                                                BorderBrush="#EF5350"
                                                FontSize="14"
                                                FontWeight="Bold"
                                                Width="30"
                                                Height="25"
                                                Command="{Binding DataContext.RemoveStepCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- 하단 추가 버튼 영역 -->
                    <Border Grid.Row="2"
                            Background="#F5F5F5"
                            BorderBrush="#BDBDBD"
                            BorderThickness="0,1,0,0"
                            Padding="10">
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                            <Button Content="➕ Add Step"
                                    Background="#4CAF50"
                                    Foreground="White"
                                    Style="{StaticResource ActionButtonStyle}"
                                    Width="120"
                                    Command="{Binding AddStepCommand}" />
                            <Button Content="🗑 Clear All"
                                    Background="#FF9800"
                                    Foreground="White"
                                    Style="{StaticResource ActionButtonStyle}"
                                    Width="120"
                                    Margin="10,0,0,0"
                                    Command="{Binding ClearAllStepsCommand}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- 오른쪽: 제어 패널 -->
            <StackPanel Grid.Column="2">
                <!-- 리스트 정보 -->
                <Border Background="#FFF3E0"
                        BorderBrush="#FF9800"
                        BorderThickness="1"
                        Padding="8"
                        Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="현재 리스트 정보"
                                   FontWeight="Bold"
                                   FontSize="10"
                                   Foreground="#F57C00"
                                   Margin="0,0,0,5" />
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,2">
                            <TextBlock Text="번호: "
                                       FontSize="9"
                                       Foreground="Gray" />
                            <TextBlock Text="{Binding CurrentListNumber}"
                                       FontSize="9"
                                       FontWeight="Bold" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,2">
                            <TextBlock Text="이름: "
                                       FontSize="9"
                                       Foreground="Gray" />
                            <TextBlock Text="{Binding CurrentListName}"
                                       FontSize="9"
                                       FontWeight="Bold" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,2">
                            <TextBlock Text="스텝 수: "
                                       FontSize="9"
                                       Foreground="Gray" />
                            <TextBlock Text="{Binding ListSteps.Count}"
                                       FontSize="9"
                                       FontWeight="Bold" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 리스트 실행 설정 -->
                <Border Style="{StaticResource SectionHeaderStyle}">
                    <TextBlock Text="실행 설정"
                               FontWeight="Bold"
                               FontSize="12"
                               Foreground="#1976D2" />
                </Border>

                <!-- 반복 모드 -->
                <Border BorderBrush="#BDBDBD"
                        BorderThickness="1"
                        Padding="10"
                        Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="반복 모드"
                                   FontWeight="Bold"
                                   FontSize="11"
                                   Margin="0,0,0,5" />
                        <RadioButton Content="단일 실행"
                                     IsChecked="{Binding IsOneShot}"
                                     GroupName="RepeatMode"
                                     Margin="0,3" />
                        <RadioButton Content="연속 반복"
                                     IsChecked="{Binding IsContinuous}"
                                     GroupName="RepeatMode"
                                     Margin="0,3" />
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,8,0,0">
                            <RadioButton Content="반복 횟수:"
                                         IsChecked="{Binding IsRepeatCount}"
                                         GroupName="RepeatMode"
                                         VerticalAlignment="Center" />
                            <TextBox Text="{Binding RepeatCount, UpdateSourceTrigger=PropertyChanged}"
                                     Width="50"
                                     Margin="5,0,0,0"
                                     IsEnabled="{Binding IsRepeatCount}"
                                     HorizontalContentAlignment="Center" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 현재 상태 표시 -->
                <Border Background="#E3F2FD"
                        BorderBrush="#2196F3"
                        BorderThickness="1"
                        Padding="10"
                        Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="현재 상태"
                                   FontWeight="Bold"
                                   FontSize="11"
                                   Foreground="#1976D2"
                                   Margin="0,0,0,5" />
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,3">
                            <TextBlock Text="실행 Step: "
                                       FontSize="10"
                                       Foreground="Gray" />
                            <TextBlock Text="{Binding CurrentStep}"
                                       FontSize="10"
                                       FontWeight="Bold"
                                       Foreground="#1976D2" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,3">
                            <TextBlock Text="진행률: "
                                       FontSize="10"
                                       Foreground="Gray" />
                            <TextBlock Text="{Binding Progress}"
                                       FontSize="10"
                                       FontWeight="Bold"
                                       Foreground="#1976D2" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal"
                                    Margin="0,3">
                            <TextBlock Text="반복: "
                                       FontSize="10"
                                       Foreground="Gray" />
                            <TextBlock Text="{Binding CurrentRepeat}"
                                       FontSize="10"
                                       FontWeight="Bold"
                                       Foreground="#1976D2" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 프로그래스 바 -->
                <ProgressBar Height="25"
                             Value="{Binding ProgressPercentage}"
                             Maximum="100"
                             Margin="0,0,0,15" />

                <!-- 실행 제어 버튼 -->

                <Button Content="⏸ 일시정지"
                        Background="#FF9800"
                        Foreground="White"
                        Style="{StaticResource ActionButtonStyle}"
                        Margin="0,0,0,8"
                        Command="{Binding PauseListCommand}"
                        IsEnabled="{Binding IsRunning}" />

                <Button Content="⏹ 중단"
                        Background="#F44336"
                        Foreground="White"
                        Style="{StaticResource ActionButtonStyle}"
                        Margin="0,0,0,15"
                        Command="{Binding AbortListCommand}"
                        IsEnabled="{Binding IsRunning}" />

                <!-- SCPI 명령어 -->
                <Border Background="#F5F5F5"
                        BorderBrush="#BDBDBD"
                        BorderThickness="1"
                        Padding="8">
                    <StackPanel>
                        <TextBlock Text="SCPI Commands"
                                   FontWeight="Bold"
                                   FontSize="10"
                                   Foreground="Gray"
                                   Margin="0,0,0,5" />
                        <TextBlock Text="Query: :LIST:CAT?"
                                   FontSize="9"
                                   FontFamily="Consolas"
                                   Foreground="#424242" />
                        <TextBlock Text="Select: :LIST:SEL {num}"
                                   FontSize="9"
                                   FontFamily="Consolas"
                                   Foreground="#424242"
                                   Margin="0,2" />
                        <TextBlock Text="Load: :LIST:CURRent?"
                                   FontSize="9"
                                   FontFamily="Consolas"
                                   Foreground="#424242"
                                   Margin="0,2" />
                        <TextBlock Text="Run: :LIST:RUN"
                                   FontSize="9"
                                   FontFamily="Consolas"
                                   Foreground="#424242"
                                   Margin="0,2" />
                    </StackPanel>
                </Border>
                <Button Content="▶ 리스트 실행"
                        Background="#4CAF50"
                        Foreground="White"
                        Style="{StaticResource ActionButtonStyle}"
                        Height="45"
                        FontSize="14"
                        Command="{Binding RunListCommand}"
                        IsEnabled="{Binding CanRun}" />
            </StackPanel>
        </Grid>

        <!-- 하단 버튼 영역 -->
        <Grid Grid.Row="3"
              Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 장비에 저장 -->
            <Button Grid.Column="0"
                    Content="📤 장비에 저장"
                    Background="#2196F3"
                    Foreground="White"
                    FontWeight="Bold"
                    Height="40"
                    FontSize="12"
                    Command="{Binding SaveToDeviceCommand}" />

            <!-- 장비에서 새로고침 -->
            <Button Grid.Column="2"
                    Content="🔄 새로고침"
                    Background="#9C27B0"
                    Foreground="White"
                    FontWeight="Bold"
                    Height="40"
                    FontSize="12"
                    Command="{Binding RefreshListsCommand}" />

            <!-- 파일로 저장 -->
            <Button Grid.Column="4"
                    Content="💾 파일로 저장"
                    Background="#607D8B"
                    Foreground="White"
                    FontWeight="Bold"
                    Height="40"
                    FontSize="12"
                    Command="{Binding SaveToFileCommand}" />

            <!-- 닫기 -->
            <Button Grid.Column="6"
                    Content="닫기"
                    Background="#757575"
                    Foreground="White"
                    FontWeight="Bold"
                    Height="40"
                    FontSize="12"
                    Command="{Binding CloseCommand}" />
        </Grid>
    </Grid>
</Window>