<Page x:Class="Source_Load_Test.View.ConnectView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Source_Load_Test.View"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="ConnectView"
      FontFamily="Noto Sans KR"
      Background="#F0F4F8">
    
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles/Border/DropShadowLight.xaml" />
                <ResourceDictionary Source="/Resources/Styles/ComboBox/ResourceDictionaryComboBox1.xaml" />
                <ResourceDictionary Source="/Resources/Styles/ComboBox/ResourceDictionaryComboBox2.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="White"
                CornerRadius="8"
                Effect="{StaticResource DropShadowLight}"
                Margin="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="70" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#4A90E2"
                        CornerRadius="8,8,0,0">
                    <TextBlock Text="Load"
                               FontSize="18"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="White" />
                </Border>

                <StackPanel Grid.Row="1"
                            Margin="15">

                    <TextBlock Text="인터페이스 선택"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5" />

                    <ComboBox Height="35"
                              Margin="0,0,0,15"
                              Padding="10,0"
                              Style="{StaticResource StyleComboDefault1}">
                        <ComboBoxItem Content="GPIB"/>
                        <ComboBoxItem Content="USB"
                                      IsSelected="True" />
                        <ComboBoxItem Content="RS232" />
                        <ComboBoxItem Content="Ethernet" />
                    </ComboBox>

                    <TextBlock Text="파라미터 설정"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5" />

                    <ComboBox Height="35"
                              Padding="10,0"
                              Style="{StaticResource StyleComboDefault1}">
                        <ComboBoxItem Content="9600 baud"
                                      IsSelected="True" />
                        <ComboBoxItem Content="19200 baud" />
                        <ComboBoxItem Content="38400 baud" />
                        <ComboBoxItem Content="115200 baud"/>
                    </ComboBox>

                </StackPanel>

                <Grid Grid.Row="2"
                      Margin="15,0,15,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Content="연결"
                            Background="#4CAF50"
                            Foreground="White"
                            FontWeight="Bold"
                            Height="45"
                            BorderThickness="0"
                            Command="{Binding Connect}"
                            CommandParameter="Load Connect" />

                    <Button Grid.Column="2"
                            Content="연결해제"
                            Background="#F44336"
                            Foreground="White"
                            FontWeight="Bold"
                            Height="45"
                            BorderThickness="0"
                            Command="{Binding Connect}"
                            CommandParameter="Load Disconnect" />
                </Grid>
            </Grid>
        </Border>

        <Border Grid.Column="2"
                Background="White"
                CornerRadius="8"
                Effect="{StaticResource DropShadowLight}"
                Margin="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="70" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#4A90E2"
                        CornerRadius="8,8,0,0">
                    <TextBlock Text="Source"
                               FontSize="18"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="White" />
                </Border>

                <StackPanel Grid.Row="1"
                            Margin="15">

                    <TextBlock Text="인터페이스 선택"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5" />

                    <ComboBox Height="35"
                              Margin="0,0,0,15"
                              Padding="10,0"
                              Style="{StaticResource StyleComboDefault1}">
                        <ComboBoxItem Content="GPIB"
                                      IsSelected="True" />
                        <ComboBoxItem Content="USB" />
                        <ComboBoxItem Content="RS232" />
                        <ComboBoxItem Content="Ethernet" />
                    </ComboBox>

                    <TextBlock Text="파라미터 설정"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="0,0,0,5" />

                    <ComboBox Height="35"
                              Padding="10,0"
                              Style="{StaticResource StyleComboDefault1}">
                        <ComboBoxItem Content="9600 baud"
                                      IsSelected="True" />
                        <ComboBoxItem Content="19200 baud" />
                        <ComboBoxItem Content="38400 baud" />       
                        <ComboBoxItem Content="115200 baud" />
                    </ComboBox>

                </StackPanel>

                <Grid Grid.Row="2"
                      Margin="15,0,15,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="15" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Content="연결"
                            Background="#4CAF50"
                            Foreground="White"
                            FontWeight="Bold"
                            Height="45"
                            BorderThickness="0"
                            Command="{Binding Connect}"
                            CommandParameter="Source Connect" />

                    <Button Grid.Column="2"
                            Content="연결해제"
                            Background="#F44336"
                            Foreground="White"
                            FontWeight="Bold"
                            Height="45"
                            BorderThickness="0"
                            Command="{Binding Connect}"
                            CommandParameter="Source Disconnect" />
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Page>